import java.io.*;

File file = new File( "/tmp/specifications/Specifications.html" );
if ( !file.exists() ) {
    throw new FileNotFoundException( "Could not find generated concordion file: " + file );
}
